spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: park_house_test
    password: no_secrets_among_devs
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  issuer: http://test-issuer
